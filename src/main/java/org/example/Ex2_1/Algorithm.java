package org.example.Ex2_1;

public class Algorithm {
    /*Will the following class compile? If not, why?*/

    /*public static <T> T max(T x, T y) {
        return x > y ? x : y;
    }*/

    //Answer: No. The greater than (>) operator applies only to primitive numeric types.
}
